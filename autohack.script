var varServer = args[0];
var numArgs = args.length


tprint("Number of Arguments: " + numArgs)
tprint("Hacking Server: " + varServer)

while(true) {
  hack(varServer);
  weaken(varServer);
  grow(varServer);
}

